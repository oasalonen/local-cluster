apiVersion: v1
kind: ConfigMap
metadata:
  name: logstash-config
  namespace: elk
  labels:
    app: logstash
data:
  pipeline.conf: |-
    input {
      beats {
        port => 5044
      }
    }

    output {
      elasticsearch { hosts => "elasticsearch:9200" }
    }